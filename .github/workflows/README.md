# Configure Firebase for auto build

`firebase login:add`
`firebase login:list`
`firebase login:use EMAIL`
`firebase use PROJECT_ID`

## Configure Github Build Action
`firebase login:ci`

Copy the token generated from `firebase login:ci` to the path below.
Go to your GitHub repo → Settings → Secrets and Variables → Actions → New repository secret